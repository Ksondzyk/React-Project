export const calendarPopupSelector = (state) => state.popupStatus;

export const calendarCellIdSelector = (state) => state.cellId;
export const calendarEventSelector = (state) => state.eventData.eventDate;
